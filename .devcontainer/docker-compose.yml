version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile

    volumes:
      - ../..:/workspaces:cached

    command: sleep infinity

    network_mode: service:db

  db:
    image: postgres:16
    restart: unless-stopped
    container_name: bibliotecamusical-postgres

    environment:
      POSTGRES_PASSWORD: Danilo1973
      POSTGRES_USER: Danilo
      POSTGRES_DB: BibliotecaMusical

    ports:
      - "5434:5432"

    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data: